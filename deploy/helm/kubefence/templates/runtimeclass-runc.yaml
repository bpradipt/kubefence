{{- if .Values.runtimeClasses.nonoRunc.enabled }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nono-runc
  labels:
    {{- include "kubefence.labels" . | nindent 4 }}
handler: nono-runc
{{- end }}
